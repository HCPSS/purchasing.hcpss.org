{%
  set classes = [
    'view-field',
    'view-name-' ~ view_name|clean_class,
  ]
%}
<div class="{{ classes|join(' ') }}">
  <div class="field-label">{{ label }}</div>
  <div class="field-value">
    {{ view }}
  </div>
</div>
